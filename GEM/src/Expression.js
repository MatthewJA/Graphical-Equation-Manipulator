// Generated by CoffeeScript 1.6.3
(function() {
  define(["coffeequate", "elementTools"], function(CQ, elementTools) {
    var Expression;
    return Expression = (function() {
      function Expression(lhs, rhs) {
        this.lhs = CQ(lhs);
        this.rhs = CQ(rhs);
      }

      Expression.prototype.toMathML = function() {
        return "<mrow>" + (this.lhs.toMathML()) + "<mo>=</mo>" + (this.rhs.toMathML()) + "</mrow>";
      };

      Expression.prototype.toElement = function() {
        return elementTools.makeExpression(this.toMathML());
      };

      return Expression;

    })();
  });

}).call(this);
