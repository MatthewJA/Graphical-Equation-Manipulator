define ->

	# Returns the singleton array of arrays of variables which are equivalent.
	# These equivalent variables are represented by strings of their labels.
	# For example, the equivalencies might be [["Ek", "Ep"], ["m2", "m3", "m4"]]

	equations = []
	-> equations